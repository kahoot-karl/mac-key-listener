{
  "name": "mac-key-listener",
  "version": "1.0.0",
  "description": "A macos key listener for node",
  "main": "dist/index.js",
  "types": "types/index.d.ts",
  "scripts": {
    "build": "swiftc -o GlobalKeyListener GlobalKeyListener.swift",
    "tsc": "rm -f types/index.d.ts && tsc --build --force"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^24.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  }
}
